class Customer {
  constructor(user) {
    this.id = user.id;
    this.name = user.name;
  }


}

export default Customer;

// domUpdates new User ({id: hotel.users.length, name: `${newCustomerName}`})